spring.application.name=campusportal-test

# In-Memory H2 Database Configuration for Tests
spring.datasource.url=jdbc:h2:mem:campus_portal_test;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# JPA/Hibernate Test Configuration
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false

# Test Profile
spring.profiles.active=dev

# Logging
logging.level.org.springframework.web=WARN
logging.level.com.saad.campusportal=WARN
