spring.application.name=campusportal

# Server Configuration
server.port=8080

# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/campus_portal_db
spring.datasource.username=root
spring.datasource.password=batmanbinsuperman
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Active Profile (for Task 4)
spring.profiles.active=dev

# Logging
logging.level.org.springframework.web=DEBUG
logging.level.com.campusportal=DEBUG